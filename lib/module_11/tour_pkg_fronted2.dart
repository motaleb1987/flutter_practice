import 'package:flutter/material.dart';
class TourPkgApp extends StatelessWidget {
  const TourPkgApp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
           // children: [
           //   Stack(
           //
           //   ),
           // ],
        ),
      ),
    );
  }
}
